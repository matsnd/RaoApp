feat(analytics): RAO-P2-065 #1 — ROI maszyny w DrillDownDrawer (główne wymaganie klienta)

Bug #1: Brak ROI (stopy zwrotu) w AnalyticsView — endpoint /stats/machine-roi
istniał ale nie był podpięty. Paradoks: ROI był tylko w Archiwum (szacunki),
a nie tam gdzie realne kwoty.

Implementacja:
- analytics.ts: dodano MachineRoi interface (mirror backend MachineRoiResponse),
  machineRoi ref, fetchMachineRoi() — równoległe z fetchMachineDetails w
  openDrillDown (best-effort: 404 dla archiwalnych nie blokuje details)
- AnalyticsView.vue: sekcja ROI w drill-down maszyny z 4 metrykami
  (ROI %, wartość zastępcza, przychód szac., dni wynajmu) + kolorowanie
  (zielony >=100%, amber >=50%, czerwony <50%, muted gdy brak replacement_value)
  + hint gdy ROI=null (wymaga wartości zastępczej w karcie artykułu)
- BACKLOG.md: P2-065 status triaged → in_progress, bug #1 i #3 oznaczone DONE

Weryfikacja:
- vue-tsc --noEmit: PASS
- npm run build: PASS (561ms)
- Playwright drill-down Ładowarka Manuscop 6.36: sekcja ROI renderuje
  (3.25% ROI, 420k wartość zastępcza, 13.65k przychód, 5 dni)
- Bug #3 (per_page=500): dropdown kontrahentów ma pełną listę (setki opcji)
- Smoke e2e 01-login: 8 passed, 2 failed (pre-existing — testy sesji/odświeżania
  failują też na czystym main bez moich zmian)

Pozostało w P2-065: bug #2 (contractor_name null), #4 (is_settled/date_to),
#5-#13 (UX/UI/P2/P3)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
