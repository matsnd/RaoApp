# TODO Faza 3: pełny CRUD router dla usług dodatkowych (AdditionalService).
# Stub — import działa, router zarejestrowany w main.py.
from fastapi import APIRouter

router = APIRouter(prefix="/additional-services", tags=["additional-services"])
