# TODO Faza 3: pełny CRUD router dla maszyn (Machine).
# Stub — import działa, router zarejestrowany w main.py.
from fastapi import APIRouter

router = APIRouter(prefix="/machines", tags=["machines"])
