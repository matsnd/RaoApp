# TODO Faza 3: pełny CRUD router dla usług (Service).
# Stub — import działa, router zarejestrowany w main.py.
from fastapi import APIRouter

router = APIRouter(prefix="/services", tags=["services"])
